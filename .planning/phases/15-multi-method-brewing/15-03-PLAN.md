---
phase: 15-multi-method-brewing
plan: "03"
type: execute
wave: 1
depends_on:
  - 15-01
  - 15-02
files_modified:
  - tests/test_brew_multimethod.py
  - app/templates/history/index.html
  - app/templates/insights/index.html
autonomous: true

must_haves:
  truths:
    - "Tests cover campaign_key derivation for espresso vs pour-over setups"
    - "Tests verify brew_setup_id is stored on recorded measurements"
    - "Tests verify legacy campaign migration renames files correctly"
    - "History page shows setup name badge on measurements that have brew_setup_id"
    - "All 193+ existing tests pass plus new tests"
  artifacts:
    - path: "tests/test_brew_multimethod.py"
      provides: "Multi-method brewing tests: campaign key, pour-over params, legacy migration"
      contains: "make_campaign_key"
    - path: "app/templates/history/index.html"
      provides: "History with optional setup context badge"
      contains: "brew_setup"
  key_links:
    - from: "tests/test_brew_multimethod.py"
      to: "app/services/optimizer_key.py"
      via: "import make_campaign_key"
      pattern: "make_campaign_key"
---

<objective>
Add tests for the multi-method campaign key system and add setup context to the history display.

Purpose: Plans 15-01 and 15-02 implemented the engine and routes. This plan verifies the behavior with dedicated tests and adds minimal UI context (setup name in history) so users can see which setup produced each brew.

Output: 15-20 new tests covering campaign key derivation, pour-over parameter set, method switching, legacy migration, and brew_setup_id recording. History shows setup name for measurements with brew_setup_id.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-multi-method-brewing/15-CONTEXT.md
@.planning/phases/15-multi-method-brewing/15-01-SUMMARY.md
@.planning/phases/15-multi-method-brewing/15-02-SUMMARY.md
@app/services/optimizer_key.py
@app/services/optimizer.py
@app/routers/brew.py
@app/templates/history/index.html
@tests/test_brew.py
@tests/test_equipment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write multi-method brew tests</name>
  <files>tests/test_brew_multimethod.py</files>
  <action>
Create `tests/test_brew_multimethod.py` with tests for the multi-method campaign key system.

Use the same test fixtures pattern as existing tests (look at tests/test_brew.py and tests/test_equipment.py for fixture patterns: `client`, `db`, `sample_bean`, etc.).

**Tests to write:**

1. **campaign_key_espresso_no_setup**: make_campaign_key("b1", "espresso", None) == "b1__espresso__none"
2. **campaign_key_espresso_with_setup**: make_campaign_key("b1", "espresso", "s1") == "b1__espresso__s1"
3. **campaign_key_pour_over**: make_campaign_key("b1", "pour-over", "s1") == "b1__pour-over__s1"
4. **parse_campaign_key_roundtrip**: parse_campaign_key(make_campaign_key("b1", "espresso", "s1")) == ("b1", "espresso", "s1")
5. **parse_legacy_key**: parse_campaign_key("old-bean-uuid") == ("old-bean-uuid", "espresso", None)
6. **is_legacy_key_old_format**: is_legacy_key("old-bean-uuid") is True
7. **is_legacy_key_new_format**: is_legacy_key("b1__espresso__none") is False
8. **legacy_migration_renames_file**: create a temp campaigns_dir with `{uuid}.json` file, call migrate_legacy_campaigns(), assert new file `{uuid}__espresso__none.json` exists and old file is gone
9. **legacy_migration_skips_existing**: if new key file already exists, don't overwrite — count should be 0
10. **optimizer_pour_over_campaign_has_bloom_param**: create OptimizerService, call get_or_create_campaign with campaign_key containing "pour-over" method, assert campaign searchspace has "bloom_weight" parameter
11. **optimizer_espresso_campaign_has_saturation**: create OptimizerService, call get_or_create_campaign for espresso, assert "saturation" in parameter names
12. **brew_setup_id_stored_on_measurement** (integration test): POST to `/brew/record` with `brew_setup_id` form field, verify Measurement row has the setup_id value
13. **brew_setup_id_null_when_not_provided** (integration test): POST to `/brew/record` without `brew_setup_id`, verify Measurement.brew_setup_id is None
14. **method_context_in_history** (integration test): create measurement with brew_setup, GET `/history`, assert setup name appears in response
15. **method_defaults_to_espresso_no_setup**: call _get_method_from_setup(None) == "espresso"

For tests 10-11 that need OptimizerService directly: instantiate with a tmp_path campaigns_dir from pytest fixtures.

For integration tests (12-14): use the `client` fixture, create a bean + setup first, then POST to brew routes.
  </action>
  <verify>
`source .venv/bin/activate && pytest tests/test_brew_multimethod.py -v --tb=short` — all new tests pass.
  </verify>
  <done>test_brew_multimethod.py exists with 15+ tests, all passing.</done>
</task>

<task type="auto">
  <name>Task 2: Add setup context badge to history page</name>
  <files>app/templates/history/index.html</files>
  <action>
Read `app/templates/history/index.html`. Find where individual measurement rows/cards are rendered.

Add an optional setup context display: if `m.brew_setup` is not None, show a small badge or label with the setup's name. Use the existing badge/tag CSS classes in the app (look for `badge` or `tag` classes in main.css).

Example (adapt to actual template structure):
```html
{% if m.brew_setup %}
  <span class="badge badge-secondary" style="font-size: var(--text-xs);">{{ m.brew_setup.name }}</span>
{% endif %}
```

Also add eager loading of brew_setup in the history router query (if not already loading it) to avoid N+1 queries. Read `app/routers/history.py` and if the query doesn't already joinedload brew_setup, add:
```python
from sqlalchemy.orm import joinedload
# in query:
.options(joinedload(Measurement.brew_setup))
```
  </action>
  <verify>
`source .venv/bin/activate && pytest tests/ -q --tb=no` — all tests pass (including test_brew_multimethod.py test 14 which checks history page shows setup name).
  </verify>
  <done>History page shows setup name badge when measurement has brew_setup_id. Router loads brew_setup eagerly.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and confirm passing count</name>
  <files>tests/</files>
  <action>
Run `source .venv/bin/activate && pytest tests/ -v --tb=short 2>&1 | tail -30`.

All 193 original tests plus new tests in test_brew_multimethod.py must pass.
Record total test count for SUMMARY.md.
  </action>
  <verify>
`source .venv/bin/activate && pytest tests/ -q --tb=no` — 0 failures.
  </verify>
  <done>All tests pass. Total test count recorded for SUMMARY.</done>
</task>

</tasks>

<verification>
- `source .venv/bin/activate && pytest tests/test_brew_multimethod.py -v --tb=short` — all new tests pass
- `source .venv/bin/activate && pytest tests/ -q --tb=no` — 0 failures total
- History page renders with setup context (verified by test 14)
</verification>

<success_criteria>
1. 15+ new tests in test_brew_multimethod.py, all passing
2. Tests cover: campaign key derivation, pour-over params, legacy migration, brew_setup_id storage, history display
3. History page shows setup name badge for measurements with brew_setup
4. All 193+ original tests still pass
5. Total test count increases to 208+
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-method-brewing/15-03-SUMMARY.md`
</output>
