---
phase: 12-manual-brew-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/measurement.py
  - migrations/versions/xxxx_add_is_manual_to_measurements.py
  - app/routers/brew.py
  - app/templates/brew/index.html
  - tests/test_brew.py
autonomous: true

must_haves:
  truths:
    - "Measurement model has is_manual Boolean column (default False)"
    - "Alembic migration adds is_manual column to measurements table"
    - "POST /brew/record accepts is_manual form field and persists it"
    - "Brew page shows bean picker dropdown and 3 mode buttons (Get Recommendation, Repeat Best, Manual Input)"
    - "Bean picker changes active bean cookie and reloads the page"
    - "Manual Input button links to /brew/manual"
  artifacts:
    - path: "app/models/measurement.py"
      provides: "is_manual column on Measurement"
      contains: "is_manual"
    - path: "migrations/versions/"
      provides: "Alembic migration for is_manual"
      contains: "is_manual"
    - path: "app/routers/brew.py"
      provides: "Modified /brew and /brew/record routes"
      contains: "is_manual"
    - path: "app/templates/brew/index.html"
      provides: "Restructured brew page with bean picker + 3 mode buttons"
      contains: "Manual Input"
  key_links:
    - from: "app/routers/brew.py"
      to: "app/models/measurement.py"
      via: "is_manual field on Measurement creation"
      pattern: "is_manual.*manual"
    - from: "app/templates/brew/index.html"
      to: "/brew/manual"
      via: "Manual Input button href"
      pattern: "/brew/manual"
---

<objective>
Foundation for manual brew: database migration, brew page restructure, and record endpoint modification.

Purpose: All subsequent manual brew work depends on the is_manual column existing and the brew page having the Manual Input entry point. This plan lays the foundation in one wave.
Output: is_manual migration applied, brew index restructured with bean picker + 3 mode buttons, /brew/record accepting is_manual flag.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-manual-brew-input/12-CONTEXT.md
@.planning/phases/12-manual-brew-input/12-RESEARCH.md
@app/models/measurement.py
@app/routers/brew.py
@app/templates/brew/index.html
@app/services/optimizer.py
@migrations/versions/e192b884d9c6_add_flavor_tags_to_measurements.py
@tests/test_brew.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_manual column + Alembic migration</name>
  <files>app/models/measurement.py, migrations/versions/xxxx_add_is_manual_to_measurements.py</files>
  <action>
1. Add `is_manual = Column(Boolean, default=False)` to the Measurement model in `app/models/measurement.py`, placed after the `is_failed` column. It should be `nullable=True` to be backward-compatible with existing rows (they'll be NULL which is falsy).

2. Generate an Alembic migration using `alembic revision --autogenerate -m "add_is_manual_to_measurements"`. If autogenerate doesn't detect it, create manually following the pattern in `e192b884d9c6_add_flavor_tags_to_measurements.py`:
   - `down_revision` should be `"e192b884d9c6"` (latest migration)
   - upgrade: `op.add_column("measurements", sa.Column("is_manual", sa.Boolean(), nullable=True, server_default=sa.text("0")))`
   - downgrade: `op.drop_column("measurements", "is_manual")`

3. Run `alembic upgrade head` to apply the migration.
  </action>
  <verify>
- `alembic upgrade head` completes without error
- `python -c "from app.models.measurement import Measurement; print(Measurement.is_manual)"` prints the column
- `alembic current` shows the new migration as head
  </verify>
  <done>Measurement model has is_manual column, migration applied successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure brew page + modify /brew/record</name>
  <files>app/routers/brew.py, app/templates/brew/index.html, tests/test_brew.py</files>
  <action>
**Brew page restructure (GET /brew):**

1. Modify `brew_index` in `brew.py` to load ALL beans (for the picker dropdown):
   ```python
   beans = db.query(Bean).order_by(Bean.name).all()
   ```
   Pass `beans` to the template context alongside existing vars.

2. Rewrite `app/templates/brew/index.html`:
   - When `no_active_bean` is True: show the existing "Pick a bean" prompt (unchanged)
   - When a bean is active, show:
     a. **Bean picker dropdown** at the top: `<select>` with all beans, pre-selecting the active bean. On change, use `fetch('/beans/' + this.value + '/activate', {method:'POST'}).then(() => window.location.reload())` to switch beans. Style with existing `.form-input` class.
     b. **Three mode buttons** in `.brew-actions`:
        - "Get Recommendation" (existing form POST, btn-primary)
        - "Repeat Best" (existing link, btn-secondary — only if has_measurements)
        - "Manual Input" — `<a href="/brew/manual" class="btn btn-secondary btn-full brew-action-btn">✏️ Manual Input</a>` (always visible when bean is active)
     c. Keep the existing empty state text for no measurements

**Modify POST /brew/record:**

3. Add `is_manual: Optional[str] = Form(None)` parameter to `record_measurement` in `brew.py`.

4. In the measurement creation block, set `is_manual=(is_manual == "true")` on the Measurement object.

**Tests:**

5. Add test `test_brew_index_shows_manual_input_button`:
   - GET /brew with active bean → response contains "Manual Input" and "/brew/manual"

6. Add test `test_brew_index_shows_bean_picker`:
   - GET /brew with active bean → response contains `<select` and the active bean's name

7. Add test `test_record_manual_measurement`:
   - POST /brew/record with `is_manual="true"` → measurement saved with `is_manual=True`
   - POST /brew/record without is_manual → measurement saved with `is_manual=False` (or falsy)

8. Run existing tests to verify nothing broke: `pytest tests/test_brew.py -v`
  </action>
  <verify>
- `pytest tests/test_brew.py -v` — all tests pass (old + new)
- Manual check: GET /brew shows bean picker, 3 buttons, Manual Input links to /brew/manual
  </verify>
  <done>Brew page has bean picker + 3 mode buttons. /brew/record persists is_manual flag. All brew tests pass.</done>
</task>

</tasks>

<verification>
- `alembic current` shows is_manual migration
- `pytest tests/test_brew.py -v` passes (all old + new tests)
- GET /brew (with active bean) HTML contains: bean picker `<select>`, "Get Recommendation", "Manual Input", "/brew/manual"
- POST /brew/record with is_manual=true creates measurement where is_manual is True
</verification>

<success_criteria>
- is_manual column exists on Measurement model with migration applied
- Brew page restructured with bean picker dropdown and 3 mode buttons
- POST /brew/record accepts and persists is_manual field
- All existing + new brew tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-manual-brew-input/12-01-SUMMARY.md`
</output>
