---
phase: 13-data-model-evolution-bean-metadata
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/routers/beans.py
  - app/templates/beans/detail.html
  - app/templates/beans/list.html
  - app/templates/beans/_bean_card.html
  - tests/test_beans.py
autonomous: true

must_haves:
  truths:
    - "User can set roast_date, process, variety when creating a bean"
    - "User can edit roast_date, process, variety on the bean detail page"
    - "Bean detail page displays process, variety, roast date in the info section"
    - "User can add a bag to a bean with optional purchase date, cost, weight, notes"
    - "Bean detail page shows all bags with their details"
    - "User can delete a bag"
    - "Bean card in list view shows process badge when set"
  artifacts:
    - path: "app/routers/beans.py"
      provides: "Updated create/edit routes + new bag CRUD routes"
      contains: "add_bag"
    - path: "app/templates/beans/detail.html"
      provides: "Enhanced metadata section + bag management section"
      contains: "roast_date"
    - path: "app/templates/beans/list.html"
      provides: "Updated create form with optional metadata fields"
      contains: "process"
  key_links:
    - from: "app/templates/beans/detail.html"
      to: "app/routers/beans.py"
      via: "POST /beans/{bean_id} with new form fields"
      pattern: "roast_date|process|variety"
    - from: "app/routers/beans.py"
      to: "app/models/bag.py"
      via: "Bag import + db.add(Bag(...))"
      pattern: "from app.models.bag import Bag"
    - from: "app/routers/beans.py"
      to: "_bean_with_shot_count"
      via: "dict includes new fields + bags"
      pattern: "roast_date|process|variety|bags"
---

<objective>
Update bean create/edit forms with enhanced metadata fields (roast_date, process, variety) and add bag management to the bean detail page.

Purpose: Fulfills requirements META-01 (bean metadata fields), META-02 (multiple bags per coffee), META-03 (cost per bag), and UI-03 (bean detail shows enhanced metadata). This is the user-facing part of Phase 13 — without it, the new data model has no UI.

Output: Updated bean forms, enhanced bean detail page with metadata + bags section, route handlers for new fields and bag CRUD, tests for all new functionality.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-data-model-evolution-bean-metadata/13-RESEARCH.md
@.planning/phases/13-data-model-evolution-bean-metadata/13-01-SUMMARY.md

Key codebase files to reference:
@app/routers/beans.py
@app/templates/beans/detail.html
@app/templates/beans/list.html
@app/templates/beans/_bean_card.html
@tests/test_beans.py
@app/models/bean.py
@app/models/bag.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bean routes and _bean_with_shot_count for enhanced metadata + bag CRUD</name>
  <files>
    app/routers/beans.py
  </files>
  <action>
**1. Update imports:**
Add at the top:
```python
from datetime import date, datetime
from app.models.bag import Bag
```

**2. Update `_bean_with_shot_count()` to include new Bean fields and bags:**
```python
def _bean_with_shot_count(db: Session, bean: Bean) -> dict:
    """Build a dict with bean fields + shot_count + bags."""
    count = db.query(func.count(Measurement.id)).filter(Measurement.bean_id == bean.id).scalar()
    return {
        "id": bean.id,
        "name": bean.name,
        "roaster": bean.roaster,
        "origin": bean.origin,
        "roast_date": bean.roast_date,
        "process": bean.process,
        "variety": bean.variety,
        "created_at": bean.created_at,
        "parameter_overrides": bean.parameter_overrides,
        "shot_count": count or 0,
        "bags": sorted(bean.bags, key=lambda b: b.created_at, reverse=True) if bean.bags else [],
    }
```

**3. Update `create_bean()` route to accept new fields:**
Add Form parameters:
```python
roast_date: str = Form(""),
process: str = Form(""),
variety: str = Form(""),
```
In the Bean constructor, add:
```python
roast_date=datetime.strptime(roast_date, "%Y-%m-%d").date() if roast_date.strip() else None,
process=process.strip() or None,
variety=variety.strip() or None,
```

**4. Update `update_bean()` route to accept new fields:**
Add same Form parameters as create. In the update logic:
```python
bean.roast_date = datetime.strptime(roast_date, "%Y-%m-%d").date() if roast_date.strip() else None
bean.process = process.strip() or None
bean.variety = variety.strip() or None
```

**5. Add bag management routes:**

```python
@router.post("/{bean_id}/bags", response_class=HTMLResponse)
async def add_bag(
    request: Request,
    bean_id: str,
    purchase_date: str = Form(""),
    cost: str = Form(""),
    weight_grams: str = Form(""),
    notes: str = Form(""),
    db: Session = Depends(get_db),
):
    """Add a bag to a bean."""
    bean = db.query(Bean).filter(Bean.id == bean_id).first()
    if not bean:
        return RedirectResponse(url="/beans", status_code=303)

    bag = Bag(
        bean_id=bean_id,
        purchase_date=datetime.strptime(purchase_date, "%Y-%m-%d").date() if purchase_date.strip() else None,
        cost=float(cost) if cost.strip() else None,
        weight_grams=float(weight_grams) if weight_grams.strip() else None,
        notes=notes.strip() or None,
    )
    db.add(bag)
    db.commit()

    return RedirectResponse(url=f"/beans/{bean_id}", status_code=303)


@router.post("/{bean_id}/bags/{bag_id}/delete")
async def delete_bag(
    request: Request,
    bean_id: str,
    bag_id: str,
    db: Session = Depends(get_db),
):
    """Delete a bag."""
    bag = db.query(Bag).filter(Bag.id == bag_id, Bag.bean_id == bean_id).first()
    if bag:
        db.delete(bag)
        db.commit()

    return RedirectResponse(url=f"/beans/{bean_id}", status_code=303)
```

**IMPORTANT conventions to follow:**
- Accept cost/weight as `str = Form("")` and parse to float manually (same pattern as overrides route) to avoid FastAPI validation errors on empty strings
- Use Form parameters with default empty strings for optional fields (matches existing pattern)
- Use `datetime.strptime(x, "%Y-%m-%d").date()` for date parsing (matches HTML `<input type="date">` output format)
- Redirect back to bean detail after bag operations (POST-redirect-GET pattern)
  </action>
  <verify>
    `python -c "from app.routers.beans import router; print('Router imports OK')"` — no errors.
    App starts without import errors: `python -c "from app.main import app; print('App OK')"`.
  </verify>
  <done>Bean create/edit routes accept roast_date, process, variety. _bean_with_shot_count includes all new fields + bags. Bag add/delete routes work. No import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update templates for enhanced metadata and bag management</name>
  <files>
    app/templates/beans/detail.html
    app/templates/beans/list.html
    app/templates/beans/_bean_card.html
  </files>
  <action>
**1. Update `app/templates/beans/detail.html`:**

Add 3 new form fields to the existing bean info form (between the "Origin" field and the "Save Changes" button):

```html
<div class="form-group">
  <label class="form-label" for="roast_date">Roast Date</label>
  <input type="date" id="roast_date" name="roast_date" class="form-input"
         value="{{ bean.roast_date.isoformat() if bean.roast_date else '' }}">
</div>
<div class="form-group">
  <label class="form-label" for="process">Process</label>
  <select id="process" name="process" class="form-input">
    <option value="">—</option>
    <option value="washed" {% if bean.process == 'washed' %}selected{% endif %}>Washed</option>
    <option value="natural" {% if bean.process == 'natural' %}selected{% endif %}>Natural</option>
    <option value="honey" {% if bean.process == 'honey' %}selected{% endif %}>Honey</option>
    <option value="anaerobic" {% if bean.process == 'anaerobic' %}selected{% endif %}>Anaerobic</option>
    <option value="other" {% if bean.process == 'other' %}selected{% endif %}>Other</option>
  </select>
</div>
<div class="form-group">
  <label class="form-label" for="variety">Variety</label>
  <input type="text" id="variety" name="variety" class="form-input"
         value="{{ bean.variety or '' }}" placeholder="e.g. SL-28, Gesha, Bourbon">
</div>
```

Add a new "Bags" section AFTER the bean info card and BEFORE the parameter overrides card:

```html
<!-- Bags -->
<div class="card mb-md">
  <h3 class="card-title" style="margin-bottom: var(--space-sm);">Bags</h3>
  <p class="text-sm text-muted mb-md">Track purchases of this coffee. Same coffee, multiple bags.</p>

  {% if bean.bags %}
    {% for bag in bean.bags %}
      <div class="card mb-sm" style="background: var(--surface-elevated, var(--surface)); padding: var(--space-sm);">
        <div style="display: flex; justify-content: space-between; align-items: center;">
          <div>
            {% if bag.purchase_date %}
              <span class="text-sm">{{ bag.purchase_date.strftime('%b %d, %Y') }}</span>
            {% endif %}
            {% if bag.cost %}
              <span class="badge badge-count">{{ "%.2f"|format(bag.cost) }}</span>
            {% endif %}
            {% if bag.weight_grams %}
              <span class="text-sm text-muted">{{ bag.weight_grams|int }}g</span>
            {% endif %}
          </div>
          <form method="post" action="/beans/{{ bean.id }}/bags/{{ bag.id }}/delete" style="margin: 0;">
            <button type="submit" class="btn btn-sm" style="color: var(--danger, #c0392b); padding: 4px 8px;"
                    onclick="return confirm('Delete this bag?')">✕</button>
          </form>
        </div>
        {% if bag.notes %}
          <p class="text-sm text-muted" style="margin-top: var(--space-xs);">{{ bag.notes }}</p>
        {% endif %}
      </div>
    {% endfor %}
  {% else %}
    <p class="text-sm text-muted">No bags recorded yet.</p>
  {% endif %}

  <button
    type="button"
    class="collapsible-toggle mt-sm"
    onclick="this.nextElementSibling.classList.toggle('open'); this.setAttribute('aria-expanded', this.nextElementSibling.classList.contains('open'))"
    aria-expanded="false"
  >
    Add Bag
    <span aria-hidden="true">&#9662;</span>
  </button>

  <div class="collapsible-content">
    <form method="post" action="/beans/{{ bean.id }}/bags">
      <div class="form-group">
        <label class="form-label" for="purchase_date">Purchase Date</label>
        <input type="date" id="purchase_date" name="purchase_date" class="form-input">
      </div>
      <div class="form-group">
        <label class="form-label" for="cost">Cost</label>
        <input type="number" id="cost" name="cost" class="form-input" step="0.01" placeholder="Optional">
      </div>
      <div class="form-group">
        <label class="form-label" for="weight_grams">Weight (grams)</label>
        <input type="number" id="weight_grams" name="weight_grams" class="form-input" step="1" placeholder="e.g. 250">
      </div>
      <div class="form-group">
        <label class="form-label" for="bag_notes">Notes</label>
        <input type="text" id="bag_notes" name="notes" class="form-input" placeholder="Optional">
      </div>
      <button type="submit" class="btn btn-secondary btn-full">Add Bag</button>
    </form>
  </div>
</div>
```

**2. Update `app/templates/beans/list.html`:**

Add optional metadata fields to the create form, between the "Origin" field and the "Add Bean" button. Use a collapsible "More Details" section to keep the form clean:

```html
<button
  type="button"
  class="collapsible-toggle"
  onclick="this.nextElementSibling.classList.toggle('open'); this.setAttribute('aria-expanded', this.nextElementSibling.classList.contains('open'))"
  aria-expanded="false"
>
  More Details
  <span aria-hidden="true">&#9662;</span>
</button>
<div class="collapsible-content">
  <div class="form-group">
    <label class="form-label" for="roast_date">Roast Date</label>
    <input type="date" id="roast_date" name="roast_date" class="form-input">
  </div>
  <div class="form-group">
    <label class="form-label" for="process">Process</label>
    <select id="process" name="process" class="form-input">
      <option value="">—</option>
      <option value="washed">Washed</option>
      <option value="natural">Natural</option>
      <option value="honey">Honey</option>
      <option value="anaerobic">Anaerobic</option>
      <option value="other">Other</option>
    </select>
  </div>
  <div class="form-group">
    <label class="form-label" for="variety">Variety</label>
    <input type="text" id="variety" name="variety" class="form-input"
           placeholder="e.g. SL-28, Gesha" autocomplete="off">
  </div>
</div>
```

**3. Update `app/templates/beans/_bean_card.html`:**

Add process badge to the card subtitle area, after roaster/origin:

```html
{% if bean.process %}
  <span class="badge" style="font-size: 0.75rem;">{{ bean.process|capitalize }}</span>
{% endif %}
```

Place this inside the existing `{% if bean.roaster or bean.origin %}` block OR make it a separate line after that block. The process badge provides at-a-glance info about the bean's processing method.

**IMPORTANT UI conventions:**
- Follow existing card/form/collapsible patterns exactly
- Use existing CSS classes: `form-group`, `form-label`, `form-input`, `btn`, `card`, `collapsible-toggle`, `collapsible-content`, `badge`, `text-sm`, `text-muted`
- Phone-first: all inputs should be full-width, 48px+ touch targets maintained
- Use `<select>` for process (fixed options), `<input type="text">` for variety (freeform)
- Use `<input type="date">` for dates (browser provides native date picker on mobile)
- Bags section uses collapsible "Add Bag" to keep detail page clean
  </action>
  <verify>
    Start the app: `uvicorn app.main:app --reload` and manually verify (or check templates parse):
    `python -c "from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('app/templates')); env.get_template('beans/detail.html'); env.get_template('beans/list.html'); print('Templates parse OK')"`
  </verify>
  <done>Bean detail page shows roast_date, process, variety in edit form. Bags section shows existing bags with delete. Collapsible "Add Bag" form. Bean create form has collapsible "More Details" with metadata fields. Bean card shows process badge.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for enhanced metadata and bag management</name>
  <files>
    tests/test_beans.py
  </files>
  <action>
Add new tests to `tests/test_beans.py` following the existing test patterns (use `client` fixture, POST form data, check response status/content):

**Bean metadata tests:**

1. **test_create_bean_with_metadata** — POST to `/beans` with name + roast_date + process=washed + variety=SL-28. Verify 200/303, then GET `/beans/{id}` and verify the detail page shows "Washed", "SL-28", and the roast date.

2. **test_create_bean_without_metadata** — POST to `/beans` with just name. Verify success (backward compatible). GET detail page, verify no metadata crash.

3. **test_update_bean_with_metadata** — Create a bean, then POST to `/beans/{id}` with updated name + roast_date + process=natural + variety=Gesha. GET detail page, verify updated values show.

4. **test_update_bean_clear_metadata** — Create a bean with process=washed, then POST to `/beans/{id}` with process="" (empty). Verify process is cleared.

**Bag management tests:**

5. **test_add_bag** — Create a bean, POST to `/beans/{id}/bags` with purchase_date, cost=15.50, weight_grams=250, notes="First bag". Verify redirect to bean detail. GET detail page, verify bag info appears (cost, weight).

6. **test_add_bag_minimal** — POST to `/beans/{id}/bags` with no optional fields (all empty strings). Verify success — a bag with no details is valid.

7. **test_delete_bag** — Create a bean, add a bag, then POST to `/beans/{id}/bags/{bag_id}/delete`. Verify redirect. GET detail page, verify bag no longer shows.

8. **test_bean_detail_shows_bags** — Create a bean, add 2 bags with different costs. GET detail page, verify both bags appear.

9. **test_delete_bean_cascades_bags** — Create bean, add bag, delete bean. Verify no orphaned bags in DB (query Bag table for the bean_id, should be empty).

**Helper for bag_id extraction:**
Since bag IDs are UUIDs generated server-side, you'll need to either:
- Query the DB directly via the `db_session` fixture (if available in the client test), OR
- Parse the bag_id from the detail page response HTML (look for the delete form action URL)

The cleanest approach: Use both `client` AND `db_session` fixtures. Create the bag via client POST, then query `db_session.query(Bag).filter(Bag.bean_id == bean_id).first()` to get the bag_id.

**IMPORTANT:** Import Bag at the top: `from app.models.bag import Bag`

Follow existing test patterns:
- Use `client.post("/beans", data={...})` for form POSTs
- Check `response.status_code` (200 for htmx, 303 for redirects — use `follow_redirects=False` or check final page)
- Use `response.text` to verify content appears in rendered HTML
  </action>
  <verify>
    `pytest tests/test_beans.py -v` — all tests pass (existing + 9 new).
    `pytest --tb=short` — all 130+ tests pass.
  </verify>
  <done>9 new tests cover bean metadata create/edit/clear and bag add/delete/cascade. All existing tests unaffected. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_beans.py -v` — all tests pass including 9 new ones
2. `pytest --tb=short` — full suite passes, zero regressions
3. Bean create form accepts and stores roast_date, process, variety
4. Bean detail page displays all enhanced metadata
5. Bag management works: add bag, view bags, delete bag
6. Bean card shows process badge in list view
7. Backward compatible: beans without metadata work fine
</verification>

<success_criteria>
- Bean create/edit routes accept roast_date, process, variety (META-01)
- Bean detail page shows enhanced metadata (UI-03)
- Multiple bags per bean with optional cost (META-02, META-03)
- Bag add/delete routes work
- _bean_with_shot_count includes all new fields
- 9 new tests pass
- All existing tests pass
- Templates follow existing UI patterns (phone-first, dark theme, collapsibles)
</success_criteria>

<output>
After completion, create `.planning/phases/13-data-model-evolution-bean-metadata/13-03-SUMMARY.md`
</output>
