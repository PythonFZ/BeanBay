---
phase: 06-analytics-exploration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/insights.py
  - app/templates/insights/index.html
  - app/templates/insights/_heatmap_chart.html
  - app/static/css/main.css
  - tests/test_insights.py
autonomous: true

must_haves:
  truths:
    - "User can view a scatter plot of grind setting vs temperature colored by taste score for the active bean"
    - "Heatmap reveals where in the parameter space good shots cluster"
    - "Failed shots are visually distinguished on the chart"
  artifacts:
    - path: "app/templates/insights/_heatmap_chart.html"
      provides: "Parameter exploration scatter/heatmap chart"
      contains: "Chart"
    - path: "app/routers/insights.py"
      provides: "Heatmap data preparation"
      contains: "heatmap_data"
  key_links:
    - from: "app/routers/insights.py"
      to: "app/templates/insights/_heatmap_chart.html"
      via: "heatmap_data context variable"
      pattern: "heatmap_data"
    - from: "app/templates/insights/index.html"
      to: "app/templates/insights/_heatmap_chart.html"
      via: "Jinja2 include"
      pattern: "include.*_heatmap_chart"
---

<objective>
Add a parameter exploration heatmap (colored scatter plot) to the existing insights page, showing grind setting vs temperature colored by taste score.

Purpose: Users can visualize where in the parameter space good shots cluster — revealing patterns like "this bean likes lower grind + higher temp" at a glance.

Output: New heatmap chart partial added to the existing insights page, with supporting data prep in the insights router.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Existing code to modify:
@app/routers/insights.py (add heatmap_data to context)
@app/templates/insights/index.html (add heatmap section)
@app/templates/insights/_progress_chart.html (Chart.js pattern to follow)
@app/static/css/main.css (chart-container class already exists)
@tests/test_insights.py (add heatmap tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Heatmap data preparation and chart template</name>
  <files>
    app/routers/insights.py
    app/templates/insights/_heatmap_chart.html
    app/templates/insights/index.html
    app/static/css/main.css
  </files>
  <action>
Modify `app/routers/insights.py` — in the `insights_page` route function, after building `chart_data`, add heatmap data preparation:

1. Build `heatmap_data` from the same `measurements` list (already queried, but need more fields). Update the measurement dict comprehension to also include `temperature`, `dose_in`, `target_yield`, `preinfusion_pct`, `saturation` fields from the raw measurements.

2. Build `heatmap_data` dict for Chart.js:
```python
heatmap_data = None
if shot_count >= 3:
    heatmap_data = {
        "points": [
            {
                "x": m["grind_setting"],
                "y": m["temperature"],
                "taste": m["taste"],
                "is_failed": m["is_failed"],
            }
            for m in measurements
        ]
    }
```

3. Pass `heatmap_data` to the template context alongside existing variables.

Create `app/templates/insights/_heatmap_chart.html`:
- Uses Chart.js scatter chart (already loaded by _progress_chart.html above it on the page — do NOT load Chart.js CDN again)
- Two datasets:
  - "Shots" — all non-failed points, colored by taste score using a gradient:
    - taste 1-3: red tones (#c84141)
    - taste 4-6: amber/muted tones (#b0a090)
    - taste 7-8: accent tones (#c87941)
    - taste 9-10: green tones (#41a060)
  - "Failed" — failed shots shown as grey X markers
- X-axis: "Grind Setting" (use actual min/max from data with small padding)
- Y-axis: "Temperature (°C)" (use actual min/max from data with small padding)
- Point radius: 8 (large touch targets for tooltip interaction)
- Tooltips show: "Grind: X, Temp: Y°C, Taste: Z"
- Use the same Chart.js theming as _progress_chart.html (dark theme colors: grid #4a4038, text #807060, tooltip #332d28)
- Canvas id: `heatmapChart` (distinct from `progressChart`)
- Wrap in `{% if heatmap_data %}...{% else %}` empty state: "Pull at least 3 shots to see the parameter map."
- Color mapping: do NOT use Chart.js matrix plugin (it's an extra dependency). Instead use a scatter chart with `pointBackgroundColor` computed per-point from taste score using a JS function that maps taste (1-10) to a color gradient.

Modify `app/templates/insights/index.html`:
- Add a new card section AFTER the "Progress" chart card and BEFORE "Quick actions":
```html
<!-- Parameter exploration -->
<div class="card mb-md">
  <h2 class="card-title mb-md">Parameter Map</h2>
  <p class="text-sm text-muted mb-md">Grind vs temperature — colored by taste score</p>
  {% include "insights/_heatmap_chart.html" %}
</div>
```

Add to `app/static/css/main.css` (if not already present):
- `.chart-container` should already exist. Verify it has `position: relative; height: 300px;` (needed for Chart.js responsive). If not, add it. Also ensure a second chart on the same page works (both use `.chart-container` independently).
  </action>
  <verify>
Start app, select a bean with 3+ shots, visit /insights. Verify:
- Progress chart still renders correctly
- Heatmap scatter chart appears below progress chart
- Points are colored by taste score (visible gradient)
- Failed shots appear as distinct markers
- Tooltips work on touch/hover
  </verify>
  <done>
Insights page shows a parameter exploration scatter chart (grind vs temperature, colored by taste) below the progress chart. Points are color-coded by taste score (red=bad, green=great). Failed shots are visually distinct. Empty state shown with < 3 shots.
  </done>
</task>

<task type="auto">
  <name>Task 2: Heatmap tests</name>
  <files>
    tests/test_insights.py
  </files>
  <action>
Add tests to the existing `tests/test_insights.py` file:

1. `test_insights_heatmap_no_data` — Set active bean cookie, create bean with 0-2 measurements. GET /insights returns 200 but response contains "Pull at least 3 shots" (heatmap empty state).

2. `test_insights_heatmap_with_data` — Create a bean with 4+ measurements (mix of taste scores). Set active bean cookie. GET /insights returns 200. Verify response contains:
   - "heatmapChart" (canvas element)
   - "Parameter Map" (section title)
   - The grind_setting and temperature values appear in the heatmap_data JSON blob

3. `test_insights_heatmap_failed_shots_distinct` — Create a bean with 3 normal + 1 failed measurement. GET /insights returns 200. Verify the heatmap_data JSON includes a point with `is_failed: true`.

Follow existing test patterns in the file. Use the `sample_bean` fixture and create measurements with all required fields.

Run `pytest tests/test_insights.py -v` — all tests pass (new + existing).
  </action>
  <verify>
`pytest tests/test_insights.py -v` — all tests pass.
`pytest` — full suite passes (no regressions).
  </verify>
  <done>
3 new heatmap tests pass covering empty state, data rendering, and failed shot distinction. Full test suite passes with no regressions.
  </done>
</task>

</tasks>

<verification>
1. `pytest` — full test suite passes
2. Insights page shows heatmap below progress chart for beans with 3+ shots
3. Heatmap colors correctly reflect taste scores (visual check)
4. Failed shots are visually distinct from normal shots
5. Empty state shown for beans with < 3 shots
6. Both charts on insights page render without conflict
</verification>

<success_criteria>
- Parameter exploration scatter chart renders on insights page
- Points colored by taste score (red→amber→green gradient)
- Failed shots visually distinguished
- Tooltips show grind, temperature, and taste
- Empty state for insufficient data
- No conflict with existing progress chart
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-exploration/06-02-SUMMARY.md`
</output>
